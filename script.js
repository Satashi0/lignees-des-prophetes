const buttons=document.querySelectorAll('[data-filter]');const cards=document.querySelectorAll('.searchable');const input=document.querySelector('#search');function apply(){const term=input.value.normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase();const active=document.querySelector('.filter .active').dataset.filter;cards.forEach(card=>{const name=card.dataset.name.normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase();const eligible=active==='all'||card.classList.contains(active);const found=!term||name.includes(term);card.classList.toggle('hidden',!eligible||!found);card.classList.toggle('match',Boolean(term)&&found&&eligible)})}buttons.forEach(button=>button.addEventListener('click',()=>{buttons.forEach(b=>b.classList.remove('active'));button.classList.add('active');apply()}));input.addEventListener('input',apply);
